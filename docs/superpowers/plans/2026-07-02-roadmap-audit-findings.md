# Roadmap Audit Findings

Working notes for docs/ROADMAP.md. Entry format defined in
docs/superpowers/plans/2026-07-02-current-state-roadmap.md.

## Coverage checklist

### Repo level (Task 1)
- [x] .gitignore, backend/.gitignore, frontend/.gitignore
- [x] git ls-files check for secrets
- [x] README.md claims vs reality
- [x] backend/requirements.txt
- [x] frontend/package.json

### Backend core (Task 2)
- [x] backend/app/main.py
- [x] backend/app/config.py
- [x] backend/app/db/database.py
- [x] backend/app/db/base_class.py
- [x] backend/app/db/crud.py
- [x] backend/app/models/transaction_model.py
- [x] backend/app/models/sync_info_model.py
- [x] backend/app/models/schemas.py
- [x] backend/app/core/exceptions.py
- [x] backend/app/core/logger.py

### Backend services + API (Task 3)
- [x] backend/app/services/transaction_service.py
- [x] backend/app/services/gmail_service.py
- [x] backend/app/services/classifier_service.py
- [x] backend/app/api/api_v1/dependencies.py
- [x] backend/app/api/api_v1/routers/transactions_router.py
- [x] backend/app/api/api_v1/routers/category_rules_router.py
- [x] backend/app/data/classification_rules.json

### Frontend (Task 4)
- [x] frontend/src/App.jsx
- [x] frontend/src/index.js
- [x] frontend/src/services/api.js
- [x] frontend/src/context/TransactionContext.jsx
- [x] frontend/src/context/DateRangeContext.jsx
- [x] frontend/src/context/CategoryContext.jsx
- [x] frontend/src/hooks/useTransactions.js
- [x] frontend/src/hooks/useTransactionData.js
- [x] frontend/src/hooks/useSummary.js
- [x] frontend/src/utils/formatters.js
- [x] frontend/src/components/TransactionList.jsx
- [x] frontend/src/components/TransactionSummary.jsx
- [x] frontend/src/components/TopSpendingCategory.jsx (untracked WIP)
- [x] frontend/src/components/CategoryFilter.jsx
- [x] frontend/src/components/CategoryEditModal.jsx
- [x] frontend/src/components/AddTransactionModal.jsx
- [x] frontend/src/components/DeleteConfirmationModal.jsx
- [x] frontend/src/components/DateRangePicker.jsx
- [x] frontend/src/components/Pagination.jsx
- [x] frontend/src/components/Header.jsx
- [x] frontend/src/components/ErrorBoundary.jsx
- [x] frontend/src/components/ErrorAlert.jsx
- [x] frontend/src/components/LoadingSpinner.jsx

## Findings

### [Medium] Live secrets sit untracked in the working tree, one `git add -f` from exposure
- **Location:** `backend/app/.env`, `backend/app/credentials.json`, `backend/app/token.json`, `backend/app/transactions.db`
- **What:** A real OpenAI API key (132 chars, `sk-` prefix, confirmed non-placeholder), a Google OAuth client secret, a live Gmail token, and a SQLite DB of real financial transactions all live in the working tree. None is tracked and none was ever committed, but `.env.example` sits directly beside `.env` in the same directory, and the only protection is gitignore patterns.
- **Impact:** No leak today, but a single `git add -f`, a gitignore edit, or a tool that bypasses gitignore publishes a live API key, Gmail access, and real financial data. Severity is Medium per the audit scale (untracked-but-present); it would be Critical if tracked or in history.
- **Verified:** `git ls-files backend/app | grep -E '\.env$|credentials\.json|token\.json|transactions\.db'` returned nothing (exit 1); `git log --oneline --diff-filter=A -- backend/app/credentials.json backend/app/token.json backend/app/.env backend/app/transactions.db` empty; broader `git log --all --diff-filter=A -- '*.env' '*credentials.json' '*token.json' '*transactions.db'` also empty; `git status --ignored -- backend/app` lists all four under "Ignored files". Key shape checked via `awk` length/prefix test without printing the value.

### [Medium] `backend/app/data/classification_rules.json` is gitignored — category rules exist only on local disk
- **Location:** `backend/.gitignore` (last line: `/app/data/classification_rules.json`)
- **What:** The classifier's rules file — an app asset the API's category-rules router reads and writes — is explicitly gitignored, so it is not version controlled and has no backup.
- **Impact:** Disk loss or an accidental delete permanently loses all user-defined category rules; a fresh clone has no rules file at all. It is also a Task 3 audit target that exists only locally.
- **Verified:** `git ls-files backend/app/data/` returns nothing; `git status --ignored -- backend/app` lists `backend/app/data/`; `backend/.gitignore` final line is `/app/data/classification_rules.json`.

### [Medium] `backend/requirements.txt` has only `>=` floors and no lockfile
- **Location:** `backend/requirements.txt:1-12`
- **What:** All 12 dependencies use `>=` floors (e.g. `fastapi>=0.104.0`, `openai>=1.0.0`, `sqlalchemy>=2.0.0`); there is no lockfile of any kind in `backend/` (no `poetry.lock`, `Pipfile.lock`, or pinned requirements).
- **Impact:** Every fresh install resolves to whatever is newest, including future breaking majors; the environment is unreproducible and a reinstall can silently break the app.
- **Verified:** `cat backend/requirements.txt` — every line is a `>=` floor; `ls backend/*.lock backend/Pipfile.lock backend/poetry.lock` found no lockfiles.

### [Medium] Frontend is built on deprecated Create React App (`react-scripts 5.0.1`)
- **Location:** `frontend/package.json:17`
- **What:** `react-scripts` is pinned at `5.0.1` (last CRA release, 2022); CRA was officially deprecated by the React team in February 2025 and receives no maintenance.
- **Impact:** Transitive dependency vulnerabilities in the build toolchain will never be patched upstream; future React/tooling upgrades are blocked until migration (e.g. to Vite). `package-lock.json` exists, so installs are at least reproducible.
- **Verified:** `frontend/package.json:17` reads `"react-scripts": "5.0.1"`; scripts block (lines 25-30) is pure CRA (`react-scripts start/build/test/eject`); `frontend/package-lock.json` present.

### [Low] Three overlapping .gitignore files with copy-paste artifacts
- **Location:** `.gitignore`, `backend/.gitignore`, `frontend/.gitignore`
- **What:** Root, backend, and frontend gitignores redundantly repeat the same patterns (`.env*`, `*.db`, `credentials.json`, `token.json`, venv, `__pycache__`). `frontend/.gitignore` contains patterns that can never match from inside `frontend/` (`/frontend/node_modules`, `/backend/node_modules`, `/backend/dist`, `/frontend/.env`, Python patterns like `venv/` and `.pytest_cache/`).
- **Impact:** Cleanup only — coverage of the actual secret files is correct in all three; the drift just makes the ignore rules harder to reason about.
- **Verified:** Read all three files in full; confirmed the secret patterns appear in root and backend gitignores and that the frontend one carries backend/Python leftovers.

### [Low] README describes a project that doesn't exist: alembic, lint tooling, env vars, file layout
- **Location:** `README.md:98,106,132,150-158,181,201,241,243,277,282`
- **What:** Concrete mismatches, each verified against the tree:
  - `README.md:106` says run `alembic upgrade head`, but `find . -name alembic.ini -not -path '*/node_modules/*'` returns nothing; tables are actually created by `Base.metadata.create_all()` at `backend/app/main.py:23`.
  - `README.md:181,201` document a `/alembic` directory and `alembic.ini` in the project structure — neither exists.
  - `README.md:98` says `cp .env.example .env` at repo root — the only `.env.example` is at `backend/app/.env.example`; there is none at the root.
  - `README.md:132` says `cp .env.example .env.local` in `frontend/` — no `frontend/.env.example` exists.
  - `README.md:150-158` documents backend env vars `API_V1_PREFIX`, `DEBUG`, `SECRET_KEY`, `ALLOWED_ORIGINS`, `DATABASE_URL` — the real `backend/app/.env.example` has `OPENAI_API_KEY`, `GMAIL_CREDENTIALS_PATH`, `LOG_LEVEL`, `CACHE_TTL`, `CACHE_MAX_SIZE`.
  - `README.md:241,243` troubleshoot via `DATABASE_URL` and `ALLOWED_ORIGINS` — same nonexistent vars.
  - `README.md:277` says lint with `flake8` and `black --check` — neither is in `backend/requirements.txt` and there is no `setup.cfg`/`pyproject.toml`/`tox.ini`.
  - `README.md:282` says `npm run lint` — `frontend/package.json` scripts are only `start`/`build`/`test`/`eject`; README also claims "ESLint with Airbnb config" but `eslintConfig` extends `react-app`.
- **Impact:** A new user following the README hits a dead end at step 5 (`alembic` not installed, no config) and configures env vars the app never reads. Docs-only, so Low.
- **Verified:** Every bullet cross-checked with `grep -n` against `README.md`, `find` for alembic/`.env.example`, `cat backend/requirements.txt`, `cat backend/app/.env.example`, and `frontend/package.json` scripts/eslintConfig blocks.

### [Critical] No authentication on any endpoint
- **Location:** `backend/app/api/api_v1/routers/transactions_router.py`, `backend/app/api/api_v1/routers/category_rules_router.py`, `backend/app/api/api_v1/dependencies.py`
- **What:** No route declares an auth dependency. Every `Depends(...)` in the API layer wires a DB session, the transaction service, or the classifier — never a user/credential check. `SECRET_KEY` and `ACCESS_TOKEN_EXPIRE_MINUTES` are declared in `config.py:13-14` but are otherwise dead (see separate finding).
- **Impact:** Every endpoint — full transaction history, transaction creation/deletion, category-rule editing — is reachable by anyone who can reach the process, with zero identity or authorization checks.
- **Verified:** `grep -rnE "Depends|auth" backend/app/api` — all 15 matches are `Depends(get_db)`, `Depends(get_classifier)`, `Depends(get_transaction_service)` or the `fastapi` imports of `Depends`; none reference auth. Cross-checked `SECRET_KEY`/`ACCESS_TOKEN_EXPIRE_MINUTES` usage repo-wide: only defined in `config.py:13-14`, never read anywhere else (`grep -rnE "SECRET_KEY|ACCESS_TOKEN_EXPIRE" backend/app` returns only those two definition lines).

### [Critical] CORS allows any origin while allowing credentials
- **Location:** `backend/app/main.py:38-44`
- **What:** `CORSMiddleware` is configured with `allow_origins=["*"]`, `allow_credentials=True`, `allow_methods=["*"]`, `allow_headers=["*"]`. The `# TODO: Update this to only allow specific origins` comment on line 40 confirms this is known-temporary, not intentional.
- **Impact:** Combined with the no-auth finding above, any web page the user's browser visits (while the local backend is running) can issue cross-origin requests to the API and read the full transaction history or mutate/delete data — the browser's same-origin protections are explicitly disabled for this API.
- **Verified:** Read `backend/app/main.py:31-44` in full; the CORS middleware block is the only security-relevant middleware registered, and no auth middleware/dependency exists anywhere in the app (see previous finding).

### [High] `get_categories` silently ignores the multi-select `categories` filter that `get_transactions`/`get_transaction_count` both honor
- **Location:** `backend/app/db/crud.py:143-185`
- **What:** `TransactionCrud.get_categories` accepts a `categories: Optional[List[dict]] = None` parameter (line 147) identical to the one `get_transactions` (line 46) and `get_transaction_count` (line 191) use to build multi-select OR conditions — but the function body (lines 154-185) never references that parameter. It only applies `date_range`/`category`/`subcategory`/`min_confidence`/`include_excluded`. To make it worse, line 178 (`categories = {}`) reassigns the local name `categories` to the output accumulator, shadowing the ignored parameter, which hides the drift from anyone reading the function in isolation.
- **Impact:** `transaction_service.py:358-361` (`TransactionService.get_categories`) passes its `categories` argument straight through to this function (confirmed by grep), and `transactions_router.py:56` calls `service.get_categories(...)` to populate the category/subcategory list returned to the frontend's filter UI. When a user has an active multi-select category filter, the transaction list (via `get_transactions`) is correctly scoped, but the category dropdown (via this function) silently reverts to showing categories from the *entire* unfiltered dataset — a real, user-visible query-result inconsistency, not just a cosmetic issue.
- **Verified:** Read `crud.py:143-185` end-to-end and compared against the near-identical logic in `get_transactions` (43-106) and `get_transaction_count` (187-243), which both do implement the `categories` OR-condition block. Confirmed the caller chain with `grep -rn "get_categories" backend/app`: `transactions_router.py:56` → `transaction_service.py:358-361` → `crud.py:144` (TransactionCrud.get_categories), with `categories` forwarded unchanged at each hop.

### [High] `transaction_exists` dedup can silently drop a legitimate second same-day purchase
- **Location:** `backend/app/db/crud.py:108-114`
- **What:** `transaction_exists` treats a transaction as a duplicate solely on `(date == date) AND (amount == amount) AND (merchant == merchant)` — an exact equality match on all three fields, with no additional signal (e.g. description, email message ID, or sub-day time tolerance) to distinguish two independent purchases.
- **Impact:** `transaction_service.py:91` calls `TransactionCrud.transaction_exists(self.db, transaction)` during ingest and skips creating the transaction if it returns `True` (confirmed by grep). Two genuine purchases sharing merchant, amount, and date — e.g. buying coffee twice in one day for the same price at the same shop, or two identical subscription-adjacent charges — collapse to a single stored row; the second is silently discarded with no error or log, producing an under-reported spending total with no user-visible signal that anything was dropped. Severity note for roadmap triage: the High-vs-Critical call is close — silently dropping a real second purchase borders on data loss (the record is never stored, not merely misdisplayed); rated High because it destroys derived accuracy for a subset of transactions rather than exposing security or losing already-stored data, but Critical is defensible.
- **Verified:** Read `crud.py:108-114` for the exact filter predicate; confirmed the only caller and its skip-on-duplicate behavior via `grep -rn "transaction_exists" backend/app` → `transaction_service.py:91` (`if transaction and not TransactionCrud.transaction_exists(self.db, transaction):`).

### [Medium] Database URL is hardcoded and CWD-relative; wrong working directory silently creates a fresh empty DB
- **Location:** `backend/app/db/database.py:7-11`
- **What:** `SQLALCHEMY_DATABASE_URL = "sqlite:///./transactions.db"` is a module-level literal — not read from `Settings`/env — and the `./` path resolves against whatever the process's current working directory happens to be at import time. `config.py` has no `DATABASE_URL` field at all (the README documents one, but per Task 1's finding that env var is fictional).
- **Impact:** Launching the server from any directory other than the expected one doesn't error — SQLite plus `main.py:23`'s `Base.metadata.create_all(bind=engine)` silently create a brand-new empty `transactions.db` in that CWD, so the app comes up healthy-looking with zero transactions. The user sees all their data "gone" (it's still in the other file), and a subsequent Gmail sync can begin re-populating the wrong file, forking the dataset. Also blocks any deployment/config story (no way to point at another path or DB engine without editing source).
- **Verified:** Read `db/database.py:1-22` in full — the URL is a literal on line 7, engine built from it on lines 8-11, and the module imports nothing from `app.config`. Read `config.py:7-43` in full — no `DATABASE_URL`/DB-path setting exists. Confirmed the silent-creation path: `main.py:23` runs `create_all` on startup against whatever file the CWD-relative URL resolves to.

### [Medium] Money columns are declared `Numeric`/`Decimal` end-to-end but SQLite physically stores them as floating-point `REAL`
- **Location:** `backend/app/models/transaction_model.py:13,23,24` (`amount`, `original_amount`, `exchange_rate` as `Numeric(10,2)`/`Numeric(10,6)`); `backend/app/models/schemas.py:26,36,37` (`Decimal` fields in the `Transaction` Pydantic model)
- **What:** The declared types are correct (`Numeric`, not `Float`, contrary to the candidate hypothesis that literal `Float` columns store money) — but SQLite has no native decimal storage class. Verified empirically: creating an in-memory SQLite table with a `Numeric(10,2)` column, inserting `Decimal('19.99')`, `Decimal('0.10')`, `Decimal('0.20')` via SQLAlchemy, then running `select typeof(amount)` shows the storage class is `real` for every row, i.e. binary floating point, not an exact decimal representation.
- **Impact:** Reads through the SQLAlchemy ORM currently round-trip cleanly back to `Decimal` (SQLAlchemy converts via `str()` on read), so no incorrect totals were observed in this check. But the on-disk representation is IEEE-754 float: any raw SQL aggregation (`SUM`/`AVG` run directly against `transactions.db`, a future reporting script, or a DB-browser tool) operates on floats and is exposed to classic binary rounding error, and the precision guarantee implied by `Numeric(10,2)` is not actually enforced by the storage layer. This is a reliability/architecture risk rather than an observed-wrong-number-today bug, hence Medium not High.
- **Verified:** Ran `backend/.venv/bin/python` script creating an in-memory `sqlite:///:memory:` engine with a `Numeric(10,2)` column, inserting three `Decimal` values, and querying `typeof(amount)` via raw SQL — all three rows report storage class `real`. Cross-checked model declarations at `transaction_model.py:13-24` and schema declarations at `schemas.py:23-49`.

### [Medium] `TransactionSummary` schema has no `top_spending_category`/`top_spending_category_amount` fields the untracked frontend component expects
- **Location:** `backend/app/models/schemas.py:59-71` (`TransactionSummary`); `frontend/src/components/TopSpendingCategory.jsx:19-21,47-51` (untracked, not modified by this audit)
- **What:** `TransactionSummary` exposes `total_spending`, `transaction_count`, `average_transaction`, `by_primary_category`, `by_subcategory`, `by_card_type`, `merchants` — no `top_spending_category` or `top_spending_category_amount` field exists anywhere in the model. The untracked `TopSpendingCategory.jsx` reads `summary.top_spending_category` and `summary.top_spending_category_amount` directly off the `transaction_summary` object returned by the API.
- **Impact:** Not a live bug — the component is untracked, unwired dead code: nothing in `frontend/src` imports or renders `TopSpendingCategory`, so the running app is unaffected today. The defect is a WIP contract mismatch that blocks completing the feature: if the component were wired into the UI as-is, both fields would always be `undefined` on the object the backend returns, `hasTopCategory` (component lines 19-21) would always be falsy, and it would permanently render its "No Data" placeholder regardless of data (failing soft via the `&&` guard, not crashing). Finishing the feature requires the backend half — schema fields plus the computation — that was never implemented.
- **Verified:** Read `schemas.py:52-71` in full (`CategorySummary` and `TransactionSummary`) — no `top_spending_category` field present. `grep -rn "top_spending_category" backend/app` returns zero matches anywhere in the backend. Read `frontend/src/components/TopSpendingCategory.jsx` in full to confirm exactly which fields it reads and how the missing-field case degrades (falls through to the no-data branch, not a crash). Confirmed the component is unwired: `grep -rn "TopSpendingCategory" frontend/src` matches only the component's own file (its definition at line 7 and `export default` at line 58) — no importer exists.

### [Medium] `update_transactions_by_merchant` swallows every exception and returns 0, indistinguishable from "no rows matched"
- **Location:** `backend/app/db/crud.py:126-140`
- **What:** The bulk-update method wraps its `db.query(...).update(...)`/`db.commit()` in a bare `try/except Exception as e:` that rolls back and `return 0` on any failure — the caught exception `e` is never logged, re-raised, or surfaced in any way.
- **Impact:** A caller (e.g. the category-rules edit flow, which uses this to retroactively reclassify all transactions for a merchant) cannot distinguish "0 transactions matched this merchant" from "the update raised an exception and was silently rolled back" (e.g. a DB lock, constraint violation, or connection error). Real failures are masked as normal no-op results, making this class of bug effectively unobservable in logs.
- **Verified:** Read `crud.py:126-140` in full; confirmed no `logger` call (or any call) exists inside the `except` block, and `db/crud.py` never imports `app.core.logger` at all (only `app.config`, `app.models.schemas`, `app.models.sync_info_model`, `app.models.transaction_model`, `sqlalchemy` — confirmed via the file's import block at lines 1-11).

### [Low] Dead auth config: `SECRET_KEY`/`ACCESS_TOKEN_EXPIRE_MINUTES` defined but never consumed
- **Location:** `backend/app/config.py:13-14`
- **What:** `SECRET_KEY: str = "your-secret-key-here"` and `ACCESS_TOKEN_EXPIRE_MINUTES: int = 30` are declared on the `Settings` model but never read anywhere else in the codebase — there is no JWT/session code that consumes them.
- **Impact:** Low by itself (dead code, not a live vulnerability, since the value is never used to sign or verify anything) — but it's a red herring: it looks like auth exists when reading `config.py`, and reinforces the false impression addressed in the Critical no-auth finding above. If someone later wires it up without changing the default, the hardcoded literal `"your-secret-key-here"` would ship as a real secret.
- **Verified:** `grep -rnE "SECRET_KEY|ACCESS_TOKEN_EXPIRE" backend/app` returns only the two declaration lines (`config.py:13` and `config.py:14`); no other file references either name.

### [Low] Two separate `declarative_base()` objects exist; one is dead and unused
- **Location:** `backend/app/db/database.py:13` vs `backend/app/db/base_class.py:3`
- **What:** `db/database.py:13` defines its own `Base = declarative_base()`, completely separate from the `Base` defined in `db/base_class.py:3`. Every model (`transaction_model.py:5`, `sync_info_model.py:3`) and `main.py:10` import `Base` from `db.base_class`, not from `db.database`. `sync_info_model.py:3` even carries a comment — `# Import existing Base instead of creating a new one` — showing the duplication was already noticed once.
- **Impact:** No live bug today (nothing imports `database.Base`, confirmed by grep), but it's a landmine: if a future model or script imports `Base` from `db.database` instead of `db.base_class`, `Base.metadata.create_all()` in `main.py` (which uses `base_class.Base`) would never create that model's table, and the mismatch would be a confusing runtime-only bug (missing table, no error at import time).
- **Verified:** Read `database.py:1-13` and `base_class.py:1-3` side by side; `grep -rn "import Base" backend/app` shows all four import sites (`main.py:10`, `sync_info_model.py:3`, `transaction_model.py:5`) resolve to `app.db.base_class`, none to `app.db.database`.

### [Low] `Transaction`/`TransactionSummary` use Pydantic v1-style `class Config`, deprecated and warning under the installed Pydantic v2.9
- **Location:** `backend/app/models/schemas.py:46-49,68-71`
- **What:** Both models define a nested `class Config: json_encoders = {...}` — the Pydantic v1 configuration style. `backend/requirements.txt:4` pins `pydantic>=2.0.0`; the installed version in `backend/.venv` is 2.9.2, which supports `class Config` only via a deprecated compatibility shim (`model_config = ConfigDict(...)` is the v2-native replacement).
- **Impact:** Cleanup only today (no functional break), but confirmed to actively emit `PydanticDeprecatedSince20` at class-definition time under the installed version, and the shim is explicitly scheduled for removal in Pydantic v3 per the warning text — a routine `pydantic>=2.0.0` floor bump (see the existing "no lockfile" finding) could pull in v3 and break class definition entirely.
- **Verified:** Ran `backend/.venv/bin/python -c "import pydantic; print(pydantic.VERSION)"` → `2.9.2`. Reproduced the exact deprecation warning by defining an equivalent minimal model with `class Config: json_encoders = {...}` under `-W error::DeprecationWarning`: raised `pydantic.warnings.PydanticDeprecatedSince20: Support for class-based \`config\` is deprecated, use ConfigDict instead. ... Deprecated in Pydantic V2.0 to be removed in V3.0`.

### [High] `average_transaction` divides by the wrong count when any transaction is excluded
- **Location:** `backend/app/services/transaction_service.py:106,131-133`
- **What:** `get_summary` builds `included_transactions = [t for t in transactions if not t.excluded]` (line 106) and correctly sums/counts over that filtered list for `total_spending` (line 131) and `transaction_count` (line 132) — but `average_transaction` (line 133) is `sum(t.amount for t in included_transactions) / len(transactions)`, dividing the included-only sum by the *unfiltered* `transactions` length.
- **Impact:** Any time at least one transaction in the requested date range is excluded, the reported average spend is understated (correct numerator, inflated denominator) for every summary the frontend renders — the headline "average transaction" stat is wrong for real users the moment they exclude even one transaction.
- **Verified:** Read `get_summary` end-to-end at `transaction_service.py:99-138`; `grep -nE "included_transactions|average_transaction" backend/app/services/transaction_service.py` confirms line 106 defines `included_transactions`, line 131/132 use it, line 133 uses `len(transactions)` instead of `len(included_transactions)`.

### [Medium] First request for an un-synced date range performs Gmail fetch + per-merchant OpenAI classification synchronously inside the GET response
- **Location:** `backend/app/services/transaction_service.py:33-54` (`get_transactions`), `77-97` (`_sync_transactions`)
- **What:** `get_transactions` (the method backing `GET /transactions`) calls `await self._should_sync_transactions(date_range)` and, if gaps exist, `await self._sync_transactions(date_range)` before querying the DB and returning — i.e. a normal read request can trigger a full Gmail query + per-email parse + per-new-merchant OpenAI classification round trip before the HTTP response is sent.
- **Impact:** The first request for a new/un-synced date range can take tens of seconds (bounded only by Gmail/OpenAI latency) and the whole request fails if either external service is down or slow, with no partial-result or sync-status signal to the client — the user just sees a hung or failed page load instead of their existing data.
- **Verified:** Read `get_transactions` (`transaction_service.py:33-54`) and `_sync_transactions` (`77-97`) in full; confirmed the `await self._sync_transactions(date_range)` call sits directly in the request path before the `TransactionCrud.get_transactions` DB read at line 52, with no background-task/job-queue indirection anywhere in the file.

### [Medium] Gmail API calls inside `_sync_transactions` are synchronous and unwrapped, blocking the asyncio event loop for every concurrent request
- **Location:** `backend/app/services/transaction_service.py:87` (`emails = self.gmail_service.get_messages(query)`); `backend/app/services/gmail_service.py:67-79` (`get_messages`), `81-105` (`_fetch_email_message`)
- **What:** `_sync_transactions` is an `async def` method, but its call to `self.gmail_service.get_messages(query)` invokes a plain synchronous method that performs blocking HTTP calls via `googleapiclient` (`.execute()`, one call to list messages plus one more per message id in `_fetch_email_message`) with no `asyncio.to_thread`/`run_in_executor` wrapper. By contrast, the classifier's OpenAI call in the same sync path *is* correctly offloaded (`classifier_service.py:148`, `await asyncio.to_thread(self.client.chat.completions.create, ...)`).
- **Impact:** Because FastAPI runs `async def` route handlers on a single-threaded event loop per worker, every synchronous Gmail HTTP call executed inside `_sync_transactions` blocks that entire worker — all other concurrent requests (including unrelated ones from other browser tabs/users) stall for the duration of the Gmail round trips, not just the requesting client. This is strictly worse than the plain sync-on-read latency above: it is a server-wide stall, not a per-request one.
- **Verified:** `grep -rnE "to_thread|run_in_executor" backend/app/services` matches only `classifier_service.py:148`; `gmail_service.py` has no such wrapping. Read `gmail_service.py:67-105` in full — both `get_messages` and `_fetch_email_message` are plain `def`, not `async def`, and call `.execute()` directly.

### [High] Silent per-email parse failures never surface, and the sync range is still marked complete
- **Location:** `backend/app/services/transaction_service.py:243-245` (missing amount/merchant), `265-267` (broad exception handler), `85-97` (`_sync_transactions` loop and `update_last_sync` call)
- **What:** `_parse_transaction` returns `None` both when `amount`/`merchant` extraction fails (line 243-245) and on any unhandled exception (line 265-267, a bare `except Exception`). In `_sync_transactions`, the loop `for email in emails: transaction = await self._parse_transaction(email); if transaction and not ...: create` simply skips `None` results with no counter, retry, or dead-letter record — and after the loop over *all* gaps finishes, `SyncInfoCrud.update_last_sync(self.db, start_date, end_date)` (line 97) unconditionally records the entire originally-requested range as synced, regardless of how many individual emails failed to parse inside it.
- **Impact:** A transaction whose email fails to parse (e.g. an NCB template variation, a truncated body, an unexpected currency string) is dropped with only a `logger.warning`/`logger.error` line — there is no retry, and because the range is marked synced, `_should_sync_transactions`'s gap detection will never re-fetch that date range again. The transaction is permanently missing from the user's history with no visible error.
- **Verified:** Read `_parse_transaction` (`197-267`) and `_sync_transactions` (`77-97`) in full. Confirmed via `grep -n "return None" backend/app/services/transaction_service.py` that lines 245 and 267 are the two `_parse_transaction` failure exits, and that `update_last_sync` at line 97 sits outside and after the per-gap `for` loop, unconditional on any per-email outcome.

### [Medium] Hardcoded fallback USD→JMD rate silently used when the currency API is unavailable
- **Location:** `backend/app/services/transaction_service.py:179-184`
- **What:** `_get_usd_to_jmd_rate` catches any failure (network error, missing key, non-200 status) from both the historical and latest currency-API calls and falls back to a hardcoded `Decimal('159')`, logged only via `logger.warning`.
- **Impact:** Any USD transaction parsed while the external currency API (`cdn.jsdelivr.net/npm/@fawazahmed0/currency-api`) is down or rate-limiting returns a JMD amount computed from a stale fixed rate rather than the real one — the stored `amount` is silently wrong, with no flag on the transaction distinguishing it from a correctly-converted one.
- **Verified:** Read `_get_usd_to_jmd_rate` (`140-184`) in full; the fallback is the last statement of the outer `except Exception as e:` block at lines 179-184, unconditionally reached whenever both HTTP attempts fail or return no rate.

### [Medium] Ingestion is hardwired to one sender and brittle HTML regexes; a bank template change silently ends ingestion
- **Location:** `backend/app/services/transaction_service.py:187-195` (`_build_gmail_query`), `209-210` (amount regex), `231-232` (merchant regex)
- **What:** `_build_gmail_query` hardcodes `from:no-reply-ncbcardalerts@jncb.com` as the only email source (line 189). `_parse_transaction` extracts the amount via `r'(?P<currency>USD|JMD)\s+(?P<amount>[\d,\.]+)'` (line 209) and the merchant via a regex matching a specific HTML structure, `r'Merchant</div></td>\s*<td[^>]*><div[^>]*>([^<]+)</div>'` (line 231), tied exactly to NCB's current email template.
- **Impact:** There is no configuration point for a second sender/bank, and any change to NCB's HTML markup (a redesign, an A/B test, a different alert template) makes the regexes stop matching. Combined with the silent-parse-failure finding above, this fails soft — no exception, no alert, just an ever-growing set of unparsed emails logged at `warning`/`error` level and marked as "synced" so they're never retried.
- **Verified:** Read `_build_gmail_query` (`186-195`) and the amount/merchant extraction block inside `_parse_transaction` (`197-233`) in full.

### [Low] Gmail OAuth token persisted via `pickle`; token refresh/creation path assumes an interactive desktop session
- **Location:** `backend/app/services/gmail_service.py:34-59`
- **What:** Credentials are deserialized with `pickle.load` (line 36) and serialized with `pickle.dump` (line 59) instead of the standard `Credentials.to_json()`/`from_authorized_user_info` JSON round trip google-auth-oauthlib recommends. When no valid token exists, `flow.run_local_server(port=0)` (line 56) starts a local HTTP server and opens a browser window from within the backend process to complete the OAuth consent flow.
- **Impact:** `pickle.load` on `token.json` executes arbitrary bytecode if that file is ever replaced/tampered with — low practical risk today since the file is only ever written by this same process (see the separate "live secrets in working tree" Medium finding for how it could otherwise leak), but it is non-standard and more fragile than JSON. Separately, `run_local_server` + browser launch only works on an interactive desktop; if this path is ever triggered from a headless/server deployment (expired token, deleted `token.json`) it will hang waiting for a browser-based consent that can never complete — and per the blocking-I/O finding above, that hang would stall the whole request-handling process, not just the one request.
- **Verified:** Read `_initialize_service` in full (`gmail_service.py:30-65`); confirmed `pickle` import at line 3 and its two call sites at lines 36/59, and `run_local_server(port=0)` at line 56 with no headless/env-flag guard.

### [High] Two independent `MerchantClassifier` singletons mean rule edits via the rules API are invisible to the classifier used for Gmail sync until process restart
- **Location:** `backend/app/api/api_v1/dependencies.py:12-19,22-31,34-41`; `backend/app/api/api_v1/routers/category_rules_router.py:14,21-22,43-44,67`
- **What:** `dependencies.py` builds classifier instances via two entirely separate mechanisms: `get_merchant_classifier()` (`@lru_cache()`, lines 17-19) is called by `get_transaction_service` (line 26) to build the `TransactionService` used by every route in `transactions_router.py` — this is the instance whose `classify_merchant` runs during Gmail sync (`transaction_service.py:247`). Separately, `get_classifier()` (lines 37-41) lazily builds and caches its own `MerchantClassifier()` into a module-level `_classifier` global, and this is the *only* instance `category_rules_router.py` ever receives (`Depends(get_classifier)` at lines 14, 21, 43, 67). `@lru_cache()` and a hand-rolled global are two unrelated caches, so `get_merchant_classifier()`'s instance and `get_classifier()`'s instance are two different `MerchantClassifier` objects — each with its own `SpecialClassificationRuleManager`, and each `SpecialClassificationRuleManager` loads `classification_rules.json` into an in-memory `self._rules` list exactly once, at `__init__` (`classifier_service.py:21-25`), and never reloads it afterward.
- **Impact:** Once a request has touched `GET /transactions` (instantiating classifier A via `get_merchant_classifier`) and a user then adds/edits a rule through the rules API (which only ever mutates classifier B's in-memory rules + `classification_rules.json` on disk), classifier A's `rule_manager._rules` is stale — it still holds the pre-edit rule set. `_get_classification_prompt` (`classifier_service.py:178-287`) builds its "SPECIAL CLASSIFICATION RULES" section from `self.rule_manager.format_rules_for_prompt()` (line 287), so every future OpenAI classification performed during Gmail sync continues to omit or use the *old* version of a rule the user just edited or deleted, until the backend process restarts and both singletons re-read the file fresh. This exactly matches the brief's "High if rule edits don't take effect until restart" case.
- **Verified:** Read `dependencies.py` in full (41 lines) and confirmed `get_merchant_classifier` and `get_classifier` are separate no-argument functions with separate cache mechanisms (`@lru_cache()` decorator vs. `global _classifier`). `grep -rnE "get_merchant_classifier|get_classifier\b" backend/app` shows `get_merchant_classifier` is only called from `dependencies.py:26`, and `get_classifier` is only imported/used by `category_rules_router.py` (lines 5,14,21,43,67) — no file calls both for the same request path. Read `SpecialClassificationRuleManager.__init__`/`_load_rules` (`classifier_service.py:21-40`) confirming rules are read from disk only at construction time, and `_get_classification_prompt` (`178-287`) confirming the prompt is built from the instance's in-memory `self.rule_manager` on every call, not re-read from disk.

### [Low] `get_gmail_service` is dead code — `get_transaction_service` builds a fresh, uncached `GmailService()` on every request instead of using it
- **Location:** `backend/app/api/api_v1/dependencies.py:12-14,25`
- **What:** `get_gmail_service` is decorated `@lru_cache()` (intended to return a singleton), but `get_transaction_service` never calls it — line 25 constructs `gmail_service = GmailService()` directly, so a fresh `GmailService` instance is created for every request that hits any transactions-router endpoint.
- **Impact:** No functional bug (each fresh `GmailService`'s `.service` property still lazily loads the same on-disk token and would only pay the OAuth/token-load cost when a Gmail call is actually made), but it's dead, misleading code — the `@lru_cache` on `get_gmail_service` implies a caching design intent that doesn't take effect, and reading `dependencies.py` in isolation suggests Gmail service instances are cached when they never are.
- **Verified:** `grep -rn "get_gmail_service" backend/app` returns only its own definition (`dependencies.py:13`) — zero callers anywhere in the codebase. Read `get_transaction_service` (`dependencies.py:22-31`) confirming line 25 calls `GmailService()` directly.

### [Medium] Classifier requests no JSON response mode from OpenAI, so ordinary non-pure-JSON replies feed the silent-transaction-drop path
- **Location:** `backend/app/services/classifier_service.py:146-168` (`_get_classification`), `170-176` (`_parse_response`)
- **What:** `_get_classification`'s `self.client.chat.completions.create(...)` call (lines 148-162) passes `model`, `messages`, and `temperature` only — no `response_format={"type": "json_object"}`. `_parse_response` then does a bare `json.loads(response)` (line 172) and raises `ClassificationError` on any decode failure.
- **Impact:** Without JSON mode, `gpt-3.5-turbo` commonly prefixes replies with prose or wraps them in markdown code fences (```json ... ```), which fails `json.loads` outright. That `ClassificationError` propagates up through `classify_merchant` (re-raised at line 144) into `_parse_transaction`'s outer `except Exception: return None` (`transaction_service.py:265-267`) — meaning a routine, non-edge-case OpenAI formatting quirk silently drops the entire transaction via the already-logged silent-parse-failure path, not just a classification retry.
- **Verified:** Read `_get_classification` (`146-168`) and `_parse_response` (`170-176`) in full; `grep -n "response_format" backend/app/services/classifier_service.py` returns no matches. Traced the exception path: `_parse_response` raises `ClassificationError` (176) → caught and re-raised by `classify_merchant`'s `except Exception as e:` (142-144) → propagates through the unguarded `await self.classifier.classify_merchant(merchant)` call at `transaction_service.py:247`, inside `_parse_transaction`'s outer try block that returns `None` on any exception (202,265-267).

### [Medium] `SpecialClassificationRuleManager.add_rule`/`edit_rule` report success even when the on-disk write fails
- **Location:** `backend/app/services/classifier_service.py:42-50` (`_save_rules`), `72-85` (`add_rule`), `87-98` (`edit_rule`)
- **What:** `_save_rules` wraps its file write in `try/except Exception as e: logger.error(...)` with no re-raise and no return value (lines 42-50). `add_rule` and `edit_rule` call `self._save_rules()` and then unconditionally `return True` (lines 84-85, 96-97) without checking whether the write actually succeeded — the in-memory `self._rules` list is already updated regardless of disk outcome.
- **Impact:** A disk error (permissions, full disk, read-only filesystem) during a rule add/edit is logged and then invisible to every caller: the HTTP response reports success (`category_rules_router.py`'s `add_rule`/`update_rule` treat any truthy return as success), the rule is usable for the remainder of the process's life (it's in memory), but is silently lost on restart since it was never persisted to `classification_rules.json`. This is the same swallowed-exception pattern already logged for `crud.py`'s `update_transactions_by_merchant` (Task 2), now confirmed in the classifier-rules persistence path too.
- **Verified:** Read `_save_rules` (42-50), `add_rule` (72-85), and `edit_rule` (87-98) in full; confirmed no code path checks a return value or exception state from `_save_rules()` before returning `True`.

### [Medium] Non-atomic rule-file writes plus silent fallback-to-defaults on any load error can permanently wipe user-added classification rules
- **Location:** `backend/app/services/classifier_service.py:27-40` (`_load_rules`), `42-50` (`_save_rules`)
- **What:** `_save_rules` writes directly to `self.rules_file` with `open(self.rules_file, 'w')` (line 47) — no temp-file-plus-rename, so a crash or power loss mid-write can leave truncated/invalid JSON on disk. `_load_rules`'s `except Exception as e:` (lines 38-40) catches any read/parse failure (including truncated JSON) and silently resets the in-memory rule set to the 9-entry `_get_default_rules()` hardcoded list — with no distinction from the file-doesn't-exist case, and no warning surfaced beyond a log line.
- **Impact:** The live `classification_rules.json` on disk currently holds 19 custom rules (verified below), 10 more than the 9-entry hardcoded default. If the file is ever corrupted (crash mid-write, disk issue) and the app restarts, all 10+ custom rules silently vanish from memory with only a log line — and the very next rule mutation (`add_rule`/`edit_rule`/`delete_rule`) calls `_save_rules()`, which overwrites the corrupted file with the 9-entry defaults, making the loss permanent and unrecoverable (no backup exists per the Task 1/2 "gitignored, no backup" finding).
- **Verified:** Read `_load_rules` (27-40) and `_save_rules` (42-50) in full — confirmed the write is a single non-atomic `open(...'w')`/`json.dump` and the load's except-block sets `self._rules = self._get_default_rules()` with no re-save-of-original-attempt or backup. Confirmed the live file has 19 rules via `python3 -c "import json; d=json.load(open('backend/app/data/classification_rules.json')); print(len(d['special_rules']))"` → `19`, versus 9 entries in `_get_default_rules()` (`classifier_service.py:52-66`).

### [Low] `delete_rule` doesn't retroactively update already-classified transactions, unlike `add_rule`/`update_rule`
- **Location:** `backend/app/api/api_v1/routers/category_rules_router.py:64-81`
- **What:** `add_rule` and `update_rule` both take a `transaction_service: TransactionService = Depends(get_transaction_service)` parameter and call `transaction_service.update_category(...)` after the rule mutation (lines 22,32 and 44,54) to bulk-overwrite matching transactions' stored category/subcategory. `delete_rule` (lines 64-81) has no `transaction_service` dependency at all and only calls `classifier.rule_manager.delete_rule(merchant)` — no DB-side update happens.
- **Impact:** Deleting a special rule leaves every previously-classified transaction for that merchant with its old category/subcategory (set while the rule was active), while any newly-ingested transaction for the same merchant is classified fresh by OpenAI without the rule's guidance — old and new transactions for the same merchant can end up in different categories with no way to reconcile via the API. This may be intentional (deletion doesn't imply retroactive re-classification is desired), so logged as an asymmetry to confirm intent on, not a confirmed defect.
- **Verified:** Read `category_rules_router.py` in full (82 lines); `grep -nE "def add_rule|def update_rule|def delete_rule|update_category|Depends\(get_transaction_service\)" backend/app/api/api_v1/routers/category_rules_router.py` confirms `add_rule`/`update_rule` both depend on `get_transaction_service` and call `update_category` (lines 22,32,44,54), while `delete_rule`'s signature (65-68) has only the `classifier` dependency.

### [Medium] Concurrent syncs of overlapping date ranges can race past the `transaction_exists` dedup check and insert duplicate rows
- **Location:** `backend/app/services/transaction_service.py:77-97` (`_sync_transactions`), `85-92` (per-email check-then-create loop)
- **What:** `_sync_transactions` computes `sync_gaps` (line 80) from the current `sync_info` state, then per gap calls `self.gmail_service.get_messages(query)` and, per email, `await self._parse_transaction(email)` (an `await` point that yields control on the event loop) before checking `not TransactionCrud.transaction_exists(self.db, transaction)` and calling `TransactionCrud.create_transaction` (lines 90-92) — a classic check-then-act sequence with no locking, no DB transaction wrapping the pair, and no unique constraint on `(date, amount, merchant)` in the schema (per Task 2's finding on `transaction_exists`) to catch a race at the DB layer.
- **Impact:** If two requests trigger `_sync_transactions` for overlapping/identical un-synced gaps concurrently (e.g. two browser tabs both loading an empty date range for the first time), both coroutines can independently fetch the same Gmail emails, and because there are `await` points between each email's parse and its existence check, interleaving on the single-threaded asyncio event loop can let both tasks pass the `transaction_exists` check for the same logical purchase before either has committed — producing two DB rows (each with a different random `uuid.uuid4()` id) for one real transaction, inflating totals. This is inferred from the code's structure (async check-then-act with no locking/constraint), not observed under load in this audit.
- **Verified:** Read `_sync_transactions` in full (`77-97`); confirmed the `await self._parse_transaction(email)` at line 90 sits between fetching `emails` and the `transaction_exists`/`create_transaction` pair, giving the event loop a yield point where another coroutine's `_sync_transactions` run could interleave. Cross-referenced Task 2's finding that `transaction_exists` (`crud.py:108-114`) is an app-level equality check with no corresponding DB `UNIQUE` constraint.

### [Low] `TopSpendingCategory.jsx` is unwired WIP — half of a feature with no route to the screen (and a tracking-status correction to Task 2's finding)
- **Location:** `frontend/src/components/TopSpendingCategory.jsx` (58 lines); `frontend/src/App.jsx:1-11` (component imports)
- **What:** From the frontend side: `App.jsx`'s import list (lines 1-11) imports `Header`, `DateRangePicker`, `TransactionList`, `TransactionSummary`, `CategoryFilter`, `AddTransactionModal` — no import of `TopSpendingCategory` anywhere, and `grep -rn "TopSpendingCategory" frontend/src` matches only the component's own definition/export lines. The component reads `summary.top_spending_category`/`summary.top_spending_category_amount` off `transaction_summary` (lines 19-21, 47-51), fields Task 2 already confirmed are absent from the backend's `TransactionSummary` schema. **Correction to Task 1/2's "untracked" characterization:** the file is no longer untracked. `git log --oneline -- frontend/src/components/TopSpendingCategory.jsx` shows it was committed at `c4d2569` ("feat: add top spending category", author Stephen Clarke, timestamp 2026-07-02 20:59:50 -0500) — a standalone commit adding only this one file (`git show --stat c4d2569`) — and `git log --oneline` places `c4d2569` *before* every docs-audit commit on this branch (`29a4506` design spec onward), including Task 2's own commit. `git ls-files frontend/src/components/TopSpendingCategory.jsx` now returns the path (tracked), and the working-tree copy is byte-identical to the committed one (`diff` against `git show c4d2569:...` is empty) — so this isn't a case of the file being re-added after Task 2 ran; it was already tracked when Task 2 wrote "untracked" into its finding.
- **Impact:** Functionally still not a live bug — there is no code path in the running app that ever mounts this component, so its missing-fields behavior (falling through to its own "No Data" placeholder, per Task 2's finding) is unreachable. But the "untracked, WIP, one `git add -f` away from being live" framing in Task 1's and Task 2's findings is factually stale as of this task's verification: the file is committed to `address-tech-debt` history now, independent of anything this audit did (Task 4 made no commits to app code — see this file's own commit hash and author/timestamp, neither of which match any audit-task commit). Flagging this for whoever assembles ROADMAP.md: cite the corrected tracked status, and note the audit's own global constraint ("TopSpendingCategory.jsx must remain untracked") was already violated by something outside this audit's control before Task 4 began.
- **Verified:** Read `App.jsx` in full (82 lines) — its six local component imports (lines 5-11) do not include `TopSpendingCategory`, and `AppContent`'s JSX (23-66) never references it. `grep -rn "TopSpendingCategory" frontend/src` returns only `components/TopSpendingCategory.jsx:7` (definition) and `:58` (`export default`). Tracking-status correction verified via `git log --oneline -- frontend/src/components/TopSpendingCategory.jsx` (one commit, `c4d2569`), `git log -1 --format="%H %an %ad %s" --date=iso c4d2569`, `git show --stat c4d2569` (58 insertions, this file only), `git ls-files frontend/src/components/TopSpendingCategory.jsx` (returns the path), and `diff <(git show c4d2569:frontend/src/components/TopSpendingCategory.jsx) frontend/src/components/TopSpendingCategory.jsx` (empty — no uncommitted changes on top).

### [High] Applying a date range truncates the end date to local midnight, dropping nearly all of that day's transactions
- **Location:** `frontend/src/context/DateRangeContext.jsx:87-91` (`setAppliedDateRangeWithValidation`), `frontend/src/components/DateRangePicker.jsx:26-32,66-78` (`handleApplyFilter`, wired to both the "Apply Filter" button and the refresh icon button); compared against `backend/app/db/crud.py:59-60,162-163,203-204` (`TransactionModel.date <= date_range.end_date`)
- **What:** Clicking "Apply Filter" (or the refresh button, which calls the identical handler) runs `setAppliedDateRange(dateRange)`, which in `setAppliedDateRangeWithValidation` unconditionally sets `endDate: startOfDay(new Date(endDate))` (line 90) — date-fns' `startOfDay` floors the date to `00:00:00.000` in the browser's local timezone. That Date object is later sent via `endDate.toISOString()` (`services/api.js:39,153`) and compared server-side with `TransactionModel.date <= date_range.end_date` (`crud.py:59-60` for the transaction list, `162-163`/`203-204` for categories/count). The same `startOfDay` truncation is applied to `startDate` too, but that only tightens the (already-inclusive) lower bound — the defect is specific to `endDate`, which needs the *end* of the day, not the start.
- **Impact:** Because almost no purchase happens at exactly local midnight, `date <= <midnight of end day>` matches essentially none of the selected end date's transactions — the transaction list, its total count (pagination), and the summary/spending totals all silently omit the entire end day the moment a user applies or refreshes a date range including "today" or any other day as the end boundary. This reproduces on every filter application, not just near a UTC/local timezone boundary, so it is broader than a one-day-only off-by-one: the end day is functionally unusable as an upper bound. (On first page load, before any Apply click, `appliedDateRange.endDate` is still the untouched `new Date()` with a real time-of-day, so today's transactions are visible until the user ever clicks Apply/Refresh — at which point they disappear.)
- **Verified:** Read `DateRangeContext.jsx:43-123` in full; confirmed `setDateRangeWithValidation` (57-74) and `setAppliedDateRangeWithValidation` (77-94) both call `startOfDay(new Date(endDate))`. Read `DateRangePicker.jsx:16-32,65-78` confirming both the "Apply Filter" button (`onClick={handleApplyFilter}`, line 67) and the refresh button (`onClick={handleApplyFilter}`, line 73) invoke the same `setAppliedDateRange` path. `grep -n "date_range" backend/app/db/crud.py` plus reading lines 56-60/159-163/200-204 confirms every query filters with `TransactionModel.date <= date_range.end_date`, a plain inclusive-at-the-instant comparison with no "end of day" adjustment on the backend either.

### [High] Editing a transaction's category without checking "create rule" silently discards the edit
- **Location:** `frontend/src/components/CategoryEditModal.jsx:39-57` (`handleSubmit`); `frontend/src/components/TransactionList.jsx:82-98` (`handleCategoryUpdate`, the modal's `onSuccess`)
- **What:** `CategoryEditModal.handleSubmit` only performs a network call when the "Create rule for all ... transactions" checkbox is checked (`if (createRule) { await addRule(...) }`, lines 45-48); it then always calls `onSuccess(transaction.id, primaryCategory, subcategory, createRule)` and closes the modal. `TransactionList.jsx`'s `handleCategoryUpdate` (the `onSuccess` handler, lines 82-98) contains no transaction-specific update call at all — the comment on lines 89-91 reads `// This would call a new API endpoint to update a transaction's category ... We don't have this function yet` — and the function body is just `await refetch()`.
- **Impact:** If a user opens the category edit modal for a single transaction, picks a new primary/sub category, and submits *without* checking "create rule", nothing is persisted anywhere: no rule is created, no transaction is updated, and no error is shown — the modal closes as if the save succeeded (same code path as a real success) and the list simply re-renders with the transaction's original, unchanged category. There is no way to re-categorize a single transaction in isolation; the only working path is the merchant-wide rule checkbox, which bulk-updates every transaction for that merchant via the backend's `update_category` (already noted in Task 3's swallowed-exception finding on that same call chain).
- **Verified:** Read `CategoryEditModal.jsx:39-57` in full — `addRule` is the only side-effecting call and it is gated behind `createRule`. Read `TransactionList.jsx:82-98` in full — `handleCategoryUpdate` never references `transactionId`/`category`/`subcategory` parameters in its body, only calls `refetch()`. `grep -rn "updateTransactionCategory\|update_category" frontend/src` matches only the commented-out placeholder at `TransactionList.jsx:91` — no per-transaction category-update API call is actually implemented or invoked anywhere in the frontend.

### [High] Mutations only `refetch()` the transaction-list query; the pagination total-count query is never refreshed
- **Location:** `frontend/src/context/TransactionContext.jsx:26-38` (`totalCount` query, no exposed refetch), `41-58,90-102` (`transactionData` query's `refetch` is the only one exposed via context); callers: `frontend/src/App.jsx:60-62` (`AddTransactionModal`'s `onSuccess`), `frontend/src/components/TransactionList.jsx:69,94,105-107` (toggle-exclude, category-edit, delete)
- **What:** `TransactionProvider` runs two independent `useQuery` calls: one for `totalCount` (`["transactionCount", filters, appliedDateRange]`, lines 26-38, used by `Pagination`) and one for `transactionData` (`transactionQueryKey(filters, appliedDateRange, pagination)`, lines 41-58). Only the second query's `refetch` is exposed through the context value (line 97); there is no React Query `invalidateQueries`/`useMutation` anywhere in the app (confirmed by grep) — every write operation (`createTransaction`, `deleteTransaction`, `toggleTransactionExclusion`, and the category-edit flow) calls this single `refetch()` after its API call and nothing else.
- **Impact:** Adding or deleting a transaction changes the true total transaction count for the active filter/date range, but the `totalCount` query used by `Pagination` (`TransactionList.jsx:361-367`) is never refetched or invalidated by these actions — it keeps serving its cached value (fresh for `staleTime: 30000` per `App.jsx:18`, and possibly longer if the tab stays open) until the user changes the date range or category filter (which changes that query's key) or 30 seconds elapse. In that window, the "Showing X to Y of Z results" text and page-number buttons are wrong, and deleting the last transaction on the last page can leave the user on a page number that no longer has any items backing it (the list query for that stale offset returns nothing, falling into the "No transactions found" empty state instead of auto-navigating back).
- **Verified:** Read `TransactionContext.jsx` in full (109 lines) — `totalCount`'s `useQuery` (26-38) destructures no `refetch`, and the provider's exported value object (90-102) has no count-refetch function, only `refetch` bound to the `transactionData` query (41-58). `grep -rn "invalidateQueries\|useMutation" frontend/src` returns zero matches anywhere in the app. Traced all four mutation call sites (`App.jsx:61`, `TransactionList.jsx:69,94,106`) — each calls the same context `refetch` and nothing else.

### [Low] Dead code: `useTransactions.js`, `useSummary.js` (also broken), and empty `CategoryContext.jsx`; `useTransactionData.js` is half-dead
- **Location:** `frontend/src/hooks/useTransactions.js` (17 lines), `frontend/src/hooks/useSummary.js` (12 lines), `frontend/src/context/CategoryContext.jsx` (0 bytes), `frontend/src/hooks/useTransactionData.js` (19 lines)
- **What:** `grep -rnE "useTransactions\b|useSummary|useTransactionData\b|transactionQueryKey|CategoryContext" frontend/src` shows every match is either a definition inside its own file or the one live usage in `TransactionContext.jsx`: `useTransactions` (the hook exported from `useTransactions.js`) has zero importers anywhere. `useSummary` has zero importers, and is additionally broken on its own terms — it imports `fetchSummary` from `../services/api` (line 2), but `services/api.js` exports no function named `fetchSummary` at all (confirmed by reading the full 194-line file and by `grep -rn "fetchSummary" frontend/src` matching only the two lines inside `useSummary.js` itself); calling this hook would throw immediately (`fetchSummary is not a function`). `CategoryContext.jsx` is a literal empty file (0 lines) with no exports and zero references anywhere in `frontend/src`. `useTransactionData.js` is split: its `useTransactionData` hook itself has zero importers, but the same file's `transactionQueryKey` helper *is* imported and used by `TransactionContext.jsx:4,47`.
- **Impact:** None of this is reachable from the running app — `App.jsx`'s import graph (`DateRangeContext` → `TransactionContext` → `services/api`/`useTransactionData`'s `transactionQueryKey`) never touches `useTransactions.js`, `useSummary.js`, or `CategoryContext.jsx`. It's pure maintenance debt: three files (plus one dead export in a fourth) that look like the intended data-fetching layer but are either superseded duplicates of logic now living in `TransactionContext.jsx`, or (for `useSummary`) leftover code aimed at an API function that was never built, or (for `CategoryContext.jsx`) an empty stub for a context that was never implemented. Anyone touching category filtering or summary fetching could reasonably start editing one of these dead files and wonder why nothing changes.
- **Verified:** `grep -rn "useTransactions\b" frontend/src`, `grep -rn "useSummary" frontend/src`, `grep -rn "CategoryContext" frontend/src`, and `grep -rn "useTransactionData\b\|transactionQueryKey" frontend/src` each read in full above. Read `services/api.js` end-to-end (194 lines) confirming no `fetchSummary` export exists. Read `CategoryContext.jsx` via the Read tool, which reports the file exists with 0 lines.

### [Low] Mixed HTTP clients in `services/api.js`: axios with a shared error interceptor for transactions, raw `fetch` with no interceptor for rules
- **Location:** `frontend/src/services/api.js:6-22` (axios instance + interceptor), `24-193` (transaction/count/create/delete/toggle functions, all via the `api` axios instance), `82-138` (`getAllRules`, `addRule`, `updateRule`, `deleteRule`, all via raw `fetch`)
- **What:** `api.js` builds one axios instance (`api`, lines 6-11) with a response interceptor (14-22) that normalizes any transaction-related error into `new Error(errorMessage)` using `error.response?.data?.detail`. The four rules-API functions (`getAllRules`/`addRule`/`updateRule`/`deleteRule`, lines 82-138) bypass this instance entirely, calling the global `fetch` directly against `${API_BASE_URL}/rules...` and hand-rolling their own `if (!response.ok) { ... throw new Error(...) }` block in each function — each one separately parses `error.detail` from the JSON body (`getAllRules` doesn't even do this: it only checks `response.ok` and throws `response.statusText`, with no attempt to read the backend's `detail` field at all).
- **Impact:** Two parallel, inconsistent error-handling paths exist in the same file for what is otherwise one API surface. `getAllRules`'s error path is strictly worse than the other three `fetch`-based functions — it surfaces a generic HTTP status text ("Not Found", "Internal Server Error") instead of the backend's actual `detail` message, unlike every other write path in the app. Not a live crash, just weaker error messages and duplicated logic that will drift further if either path is touched.
- **Verified:** Read `api.js` end-to-end (194 lines); confirmed the axios instance/interceptor (6-22) is used by every function that calls `api.get/post/patch/delete`, while `getAllRules`/`addRule`/`updateRule`/`deleteRule` (82-138) each call the ambient `fetch` directly and duplicate their own error handling, with `getAllRules` (82-88) the only one of the four that never inspects the response body on failure.

### [Low/gap] Currency display hardcodes JMD; the original currency, amount, and exchange rate the backend returns are never shown
- **Location:** `frontend/src/utils/formatters.js:1-6` (`formatCurrency`); `backend/app/models/schemas.py:33-36` (`Transaction.original_currency`, `original_amount`, `exchange_rate`, `exchange_rate_date`); `frontend/src/components/DeleteConfirmationModal.jsx:72` (raw, unformatted amount)
- **What:** `formatCurrency` (`formatters.js:1-6`) always formats with `new Intl.NumberFormat("en-US", { style: "currency", currency: "JMD" })` — there is no parameter or branch for any other currency. The backend's `Transaction` schema carries `original_currency`, `original_amount`, `exchange_rate`, and `exchange_rate_date` (`schemas.py:33-36`) specifically for transactions that were converted from USD, but `grep -rnE "original_amount|original_currency|exchange_rate" frontend/src` returns zero matches — none of these fields is read anywhere in the UI. Separately, `DeleteConfirmationModal.jsx:72` renders `JMD {transaction.amount}` as a raw string interpolation instead of calling `formatCurrency`, so that one dialog shows an unformatted number (no thousands separator, no fixed 2-decimal places) while every other view of the same data uses `formatCurrency`.
- **Impact:** A user with a USD-denominated card transaction sees only the converted JMD figure everywhere in the app, with no way to see the original USD amount or the exchange rate that was applied — relevant given the separate Medium finding that the conversion rate can silently fall back to a hardcoded stale value when the currency API is down; the UI gives the user no signal to notice or question a bad conversion. The delete-confirmation inconsistency is purely cosmetic (a raw number vs. a formatted currency string in one dialog) but adds to the general lack of a single, consistent money-formatting path.
- **Verified:** Read `formatters.js` in full (14 lines) — `currency: "JMD"` is a literal with no parameterization. `grep -rn "original_amount\|original_currency\|exchange_rate" frontend/src` returned no matches. Read `schemas.py:23-49` confirming the four currency-conversion fields exist on the API's `Transaction` model. Read `DeleteConfirmationModal.jsx:64-77` confirming line 72 interpolates `transaction.amount` directly rather than calling `formatCurrency`.

### [Low] `TransactionList`'s filter-summary text references `filters.category`/`filters.subcategory`, which no longer exist on the filters object
- **Location:** `frontend/src/components/TransactionList.jsx:161-165`; `frontend/src/context/TransactionContext.jsx:11-13` (`filters` state shape)
- **What:** The "Showing N transactions..." caption reads `{filters.category && ...}` and `{filters.subcategory && ...}` (lines 163-164), but `TransactionContext.jsx`'s `filters` state (lines 11-13) is `{ categories: [] }` — an array of `{category, subcategory}` objects for the multi-select filter (per the comment on line 12 and the commit history's "implement multi-select category filtering" migration) — there is no top-level `category`/`subcategory` field on `filters` anywhere in the current code.
- **Impact:** Purely cosmetic: `filters.category` and `filters.subcategory` are always `undefined`, so the "in {category} - {subcategory}" qualifier can never render, regardless of what category filter is actually active. The underlying filtering itself works correctly (it uses `filters.categories`, confirmed live in `TransactionContext.jsx:30-36,47-55`); only this leftover descriptive text from the pre-multi-select single-category UI is dead.
- **Verified:** Read `TransactionList.jsx:152-167` and `TransactionContext.jsx:9-38` side by side; confirmed `filters` never has `category`/`subcategory` keys set anywhere in `TransactionContext.jsx` (`grep -n "filters\." frontend/src/context/TransactionContext.jsx` shows only `filters.categories`/spreads of `filters` used in query functions, no singular `category`/`subcategory` assignment).

## Architecture notes

- **Request flow:** `main.py` builds a single `FastAPI` app, registers `transactions_router` and `category_rules_router` under `settings.API_V1_STR` (`/api/v1`), and wires a single `CORSMiddleware`. A `lifespan` context manager runs `Base.metadata.create_all(bind=engine)` on startup — there is no migration tool; schema changes require a fresh DB file or manual ALTERs (consistent with Task 1's README-alembic finding).
- **Session/DI pattern:** `db/database.py` owns the SQLite engine (`sqlite:///./transactions.db`, CWD-relative, not env-configurable — see Medium finding above) and a `get_db()` generator that yields a `SessionLocal()` and closes it in a `finally` block; `api/api_v1/dependencies.py` exposes this as a FastAPI `Depends(get_db)` used to construct the service layer per-request.
- **Two declarative bases exist** (`db/base_class.py` and `db/database.py`); only `base_class.Base` is actually wired into models and `main.py`'s `create_all()` call (see Low finding above) — `database.Base` is inert dead code.
- **Sync-gap model:** two tables total — `transactions` (`TransactionModel`, one row per purchase, denormalized `primary_category`/`subcategory`/`card_type` as plain strings, `id` as a `String(36)` UUID) and `sync_info` (`SyncInfoModel`, a *single* row keyed `id="last_sync"` holding `last_sync_date`/`start_date`/`end_date`). `SyncInfoCrud.get_sync_gaps()` (crud.py:389-427) diffs a requested `DateRange` against the stored `(start_date, end_date)` to compute which sub-ranges still need fetching from Gmail (gap-before, gap-after), and `_calculate_optimal_sync_range`/`_apply_sync_limits` (crud.py:304-386) apply a sliding window bounded by `settings.MAX_SYNC_DAYS`/`SYNC_WINDOW_DAYS`/`MIN_SYNC_OVERLAP_HOURS` so the tracked range doesn't grow unbounded. Ingest-time dedup against already-stored transactions is a separate, simpler mechanism: `transaction_exists()` (crud.py:108-114), an exact `(date, amount, merchant)` match — see High finding on its collision risk.
- **Where categories come from:** there is no `categories` table. `primary_category`/`subcategory` are free-text columns set per-transaction (originally by the classifier service, out of this task's scope) and `get_categories()`/`get_transaction_count()` derive the distinct category→subcategory map and counts live from the `transactions` table by `SELECT DISTINCT`. Category *rules* (merchant → category/subcategory mappings used to drive future classification) are a completely separate, file-based store — `backend/app/data/classification_rules.json` (gitignored per Task 1's finding) — not modeled in the SQL schema at all.
- **Config surface:** `config.py`'s single `Settings(BaseSettings)` object is populated from `backend/app/.env` (`case_sensitive = True`). Confirmed-live fields: `API_V1_STR`, `PROJECT_NAME`, `OPENAI_API_KEY`, `GMAIL_CREDENTIALS_PATH`/`GMAIL_TOKEN_PATH`/`GMAIL_SCOPES`, `CACHE_TTL`/`CACHE_MAX_SIZE` (consumed by `classifier_service.py:125-126`'s `TTLCache`), `LOG_LEVEL` (consumed by `core/logger.py:13`), `MAX_SYNC_DAYS`/`SYNC_WINDOW_DAYS`/`MIN_SYNC_OVERLAP_HOURS` (consumed by the sync-gap logic above). Confirmed-dead fields: `SECRET_KEY`, `ACCESS_TOKEN_EXPIRE_MINUTES` (Low finding above). `MASTERCARD_TYPE`/`VISA_TYPE` are declared but not checked in this task's file set (consumer, if any, is in Task 3's scope).
- **Type boundary between layers:** `TransactionModel.id` is `String(36)` in the DB; `schemas.Transaction.id` is `uuid.UUID` in the API layer. Every `crud.py` call site does an explicit `str(transaction_id)` (write/filter) or accepts a `uuid.UUID` and stringifies it before querying — consistent throughout the file today, but there's no DB-level UUID type or CHECK constraint enforcing the format, so the consistency is a convention, not a guarantee.
- **Indexing:** contrary to one candidate hypothesis, `date`, `merchant`, `primary_category`, and `subcategory` all already carry `index=True` (`transaction_model.py:12,14-16`) — the columns actually used by every list/filter query in `crud.py` are indexed. No composite index exists for common combined filters (e.g. `date` + `primary_category` together), which SQLite's query planner can only partially exploit via single-column indexes, but this is a minor tuning note, not a missing-index defect.
- **Service/DI layer (Task 3):** `api/api_v1/dependencies.py` is the sole composition root — it builds `TransactionService(gmail_service, classifier, db)` per request via `get_transaction_service` and a separate bare `MerchantClassifier` via `get_classifier` for the rules router (see High finding on the resulting split-brain rule cache). `TransactionService` is a thin orchestrator: `get_transactions` → sync-if-needed (`_should_sync_transactions`/`_sync_transactions`) → `TransactionCrud` read → Pydantic conversion; `_sync_transactions` → `GmailService.get_messages` (sync/blocking) → `_parse_transaction` (regex extraction + `MerchantClassifier.classify_merchant`) → `TransactionCrud.transaction_exists`/`create_transaction`. `MerchantClassifier` wraps a synchronous `openai.Client` call in `asyncio.to_thread` (correctly non-blocking) plus an in-process `TTLCache` (`CACHE_TTL`/`CACHE_MAX_SIZE`, confirmed wired) and a `SpecialClassificationRuleManager` that owns `classification_rules.json` (load-once, non-atomic save — see Medium/High findings).
- **Rules API atomicity:** `category_rules_router.py`'s `add_rule`/`update_rule` perform two independent, unguarded side effects per request — write `classification_rules.json` via `rule_manager` (in-memory + disk), then bulk-`UPDATE` matching transactions via `TransactionService.update_category` → `TransactionCrud.update_transactions_by_merchant` (Task 2's swallowed-exception finding). Neither step is wrapped in a shared transaction or compensating action if the other fails; `delete_rule` skips the DB step entirely (see Low asymmetry finding). None of the three write routes (`POST`/`PUT`/`DELETE` on `/rules`) declare a response model reflecting partial failure — a JSON-save failure that's silently swallowed by `_save_rules` still returns HTTP 200 to the caller.
- **No rate-limit/cost guard on classification:** `MerchantClassifier.classify_merchant` calls OpenAI once per cache-miss merchant with no batching, throttling, retry/backoff, or spend cap — a large gap sync (many new merchants at once, e.g. after first-ever sync or a long-idle period) issues that many sequential OpenAI calls back-to-back with no guard against rate-limit errors or runaway cost.

## Gap candidates

- No database migration tooling at all (README's alembic is fictional); schema comes from `create_all()`, which never alters existing tables.
- No backup/versioning story for `classification_rules.json` or `transactions.db` — all user data lives in gitignored local files.
- No secret-scanning or pre-commit guard despite live secrets sitting in the working tree.
- No linting/formatting actually wired up (backend or frontend) despite README claims.
- No authentication layer of any kind exists to build on — adding auth is a from-scratch effort, not a config flip (see Critical findings).
- No structured logging/observability around silent-failure paths (e.g. `update_transactions_by_merchant`'s swallowed exceptions) — errors that should be loud are currently invisible.
- No background job/queue mechanism for Gmail sync — every sync-triggering request pays the full Gmail+OpenAI latency inline; a task queue (Celery/RQ/FastAPI `BackgroundTasks` + polling) would decouple "fetch fresh data" from "answer this request."
- No rate-limit/retry/cost guard around OpenAI classification calls (see architecture note above) — worth a backlog item before any bulk backfill or wider usage.
- No atomic-write helper (temp file + rename) used anywhere JSON is persisted (`classification_rules.json`) — a small shared utility would remove the corrupt-then-silently-reset failure mode.
- No per-transaction re-classification — confirmed absent. The only category-change path is `CategoryEditModal` → `addRule`, which is merchant-wide (and, per the new High finding above, silently no-ops unless "create rule" is checked); there is no API call or UI control anywhere in `frontend/src` that updates a single transaction's category in isolation — `grep -rn "updateTransactionCategory\|update_category" frontend/src` matches only a commented-out placeholder call at `TransactionList.jsx:91` (`// await updateTransactionCategory(transactionId, category, subcategory);`), which is never uncommented or implemented.
- No sync status or manual-sync control — confirmed absent. `grep -rniE "sync" frontend/src` matches only unrelated identifiers (`toggleTransactionExclusion`, `handleSubmit`, etc.); there is no sync-progress indicator, last-synced timestamp, or manual "sync now" button anywhere in the UI, consistent with the backend Medium finding that sync happens invisibly inline with `GET /transactions`.
- No month-over-month or any historical/trend comparison view — confirmed absent. `grep -rniE "month|trend|compar|history" frontend/src` matches only a code comment about picking the 25th of the month (`DateRangeContext.jsx:9-10`) and the string `"month"` inside `Intl.DateTimeFormat` options (`formatters.js:11`); no comparison-across-periods UI exists.
- No CSV/data export — confirmed absent. `grep -rniE "csv|export|download" frontend/src` matches only unrelated `export const`/`export default` statements; there is no download or export control anywhere in the app.
- No search-by-merchant box — confirmed absent. `grep -rniE "search" frontend/src` matches only `URLSearchParams` in `services/api.js`; filtering is limited to the category dropdown and date range, with no free-text merchant search anywhere in the UI.

## Feature candidates

- `top_spending_category`/`top_spending_category_amount` on `TransactionSummary`: the frontend half of this feature already exists (untracked `TopSpendingCategory.jsx`); the backend computation and schema fields are the missing half.
- Sync-status/progress signal for the frontend (e.g. a `syncing: bool`/last-sync-timestamp field) so a slow first-load isn't indistinguishable from a hang — would also make the sync-on-read Medium finding easier to live with even before it's made async.

## Unverified/rejected

- **"Amounts stored as Float vs Decimal in Pydantic" (brief's Step 3 hypothesis), as literally stated — rejected.** `transaction_model.py:13,23` declares `amount`/`original_amount` as `Numeric(10,2)` (not `Float`), and `schemas.py:26,36` declares them as `Decimal` in Pydantic — the two layers agree on type. The real, different issue (SQLite physically storing `Numeric` as floating-point `REAL`) is logged above as its own Medium finding instead.
- **"Missing indexes on date/merchant used by every query" — rejected.** `transaction_model.py:12,14` shows `date = Column(DateTime, index=True)` and `merchant = Column(String, index=True)`; `primary_category`/`subcategory` are also indexed (lines 15-16). All four are indexed already; see the Indexing architecture note above for the (minor) composite-index nuance.
- **"Classifier's OpenAI call blocks the event loop" (part of the brief's blocking-I/O candidate) — rejected for the classifier specifically.** `classifier_service.py:148` wraps `self.client.chat.completions.create` in `await asyncio.to_thread(...)`, correctly offloading the blocking OpenAI SDK call to a worker thread. The blocking-I/O finding that *is* confirmed applies to `GmailService.get_messages`/`_fetch_email_message` only (see Medium finding above), which have no such wrapping.
- **TTLCache config usage — re-confirmed, not re-logged.** Task 2 already verified via grep that `classifier_service.py:125-126` reads `settings.CACHE_MAX_SIZE`/`settings.CACHE_TTL`; this task's end-to-end read of `MerchantClassifier.__init__` (`122-128`) confirms the same and adds no new defect — `CACHE_TTL=86400`/`CACHE_MAX_SIZE=1000` in `backend/app/.env.example` are genuinely consumed.
- **"CACHE_TTL/CACHE_MAX_SIZE shadowed by literals" — rejected.** `grep -rnE "CACHE_TTL|CACHE_MAX_SIZE" backend/app` shows both are read by `classifier_service.py:125-126` (`TTLCache(maxsize=settings.CACHE_MAX_SIZE, ttl=settings.CACHE_TTL)`) — genuinely wired to config, not shadowed by hardcoded literals. (`classifier_service.py` itself is Task 3's file; confirmed here only via grep for the config-consumption cross-check, not read end-to-end.)
- **`id` as `String(36)` vs Pydantic `uuid.UUID` — investigated, not logged as a bug.** Every `crud.py` call site (`create_transaction`, `get_transaction_by_id`, `set_exclusion`, `delete_transaction`) consistently converts with `str()`/accepts `uuid.UUID` before querying; no call site was found that compares raw types without conversion. Logged as an architecture note (type-boundary convention, not a guarantee) rather than a finding, since no actual defect was observed within this task's file set.
- **SQL injection via `ilike(f"%{cat_name}%")` in `crud.py:72-84,216-228` — checked, not a vulnerability.** The f-string only builds the `LIKE` pattern text; it is passed to SQLAlchemy's `.ilike()` as a bound parameter, not interpolated into raw SQL. A user-supplied `%`/`_` could broaden their own match unexpectedly (wildcard-injection, cosmetic) but this is not a security defect and wasn't logged as one.
- **Pagination count/list filter consistency — checked, not a bug.** The brief's candidate that the count query might use different filters than the list query is rejected: `TransactionContext.jsx`'s `totalCount` query (26-38) and `transactionData` query (41-58) both spread the exact same `filters` state and `appliedDateRange` values into `getTransactionCount`/`fetchTransactions`. Offset math is also correct: `goToPage` (`TransactionContext.jsx:80-87`) computes `(page - 1) * pagination.limit`, matching `Pagination.jsx:74,78`'s displayed range. (The pagination *is* broken in a different way — see the new High finding above on mutations not refetching `totalCount`.)
- **`AddTransactionModal` negative/non-numeric amount — checked, not currently exploitable via the UI.** The amount `<input>` (`AddTransactionModal.jsx:151-163`) is `type="number" step="0.01" min="0" required` with no `noValidate` on the `<form>` (confirmed via `grep -rn "noValidate" frontend/src`, no matches) or anywhere in the tree, so native HTML5 constraint validation blocks submission of empty, negative, or non-numeric values before `handleSubmit` runs. The backend's `TransactionCreate.amount` schema (`schemas.py:101`) independently enforces `Decimal = Field(decimal_places=2, gt=0)`, so even a value that bypassed the browser (e.g. a hand-crafted request) would be rejected server-side with a 422. Gap noted: there is no *JavaScript*-level check in `handleSubmit` itself (`parseFloat(formData.amount)` is used unguarded) — it's a defense-in-depth gap, not a currently reachable bug, so not logged as a finding.
- **Error states when the backend is down — checked, handled gracefully.** `api.js`'s axios interceptor (14-22) falls back to `"An unexpected error occurred"` when `error.response` is absent (i.e., no response at all, as with a fully down backend), and `TransactionSummary.jsx:12`/`TransactionList.jsx:117-123` both render `ErrorAlert` with a "Try again" button wired to `refetch()`. No crash or blank-screen path was found.
